import { PerformanceConverterPipe } from './performance-converter.pipe';

describe('PerformanceConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PerformanceConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
